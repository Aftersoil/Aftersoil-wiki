# 尺寸和边框

1. 尺寸属性

  作用: 设置元素的宽和高

## 属性
```
width: 宽度
height: 高度
注意，如果不写宽高，各个元素默认的宽高是多少
  1. 块级不写宽，默认宽度沾满父容器宽度 100%
  2. 块级不写高，默认高度靠内容撑起来
  3. 行内元素，设置宽高无效，它的宽高靠内容撑
  4. 自带宽高属性的元素，设置宽高有效(img， table)

max-width: 最大宽度
min-width: 最小宽度
max-height: 最大高度
min-height: 最小高度
取值: px 为单位的数字和父元素的 % 比
```

## 溢出处理
```
  当内容较大，元素区域较小的时候，就会发生移除，默认就是纵向溢出
  overflow:
    取值: visible 默认值，溢出部分可见
          hidden 溢出部分隐藏
          scroll 不管是否溢出，x和y轴方向都添加滚动条
          auto   只有溢出才有滚动条，不溢出没有
  overflow-x: 设置水平轴滚动条
  overflow-y: 设置垂直轴滚动条
  如何设置成横向溢出
  内部容器，设置宽度，大于外部容器的宽度
```

## 边框
```
1. 边框的简写方式
  solid 实线
  dotted 点点虚线
  dashed 断线虚线
```

## 附加知识点 1. 单位

```
单位: 
  px 像素
  in 英寸  1in=2.54cm
  pt 磅值  1pt=1/72in  多用于设置字体大小
  cm 厘米  
  mm 毫米  
  项目中，为了页面中可以在 pc pad phone 都正常显示我们一般会使用相对单位
  em  以父元素的数值当做基本单位
  rem 以html的数值当做基本单位
  %   
```
